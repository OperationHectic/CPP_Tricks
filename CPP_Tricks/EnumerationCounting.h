/* This enumeration structure alawys you to keep adding new constants into the enumeration and always keeps 
 * of how many there are with the COUNT constant track*/

enum
{
	ENUM1,
	ENUM2,
	ENUM3,
	ENUM4,
	ENUM5,
	COUNT
};
